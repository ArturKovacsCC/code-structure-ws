
// Task 1
// Write a function called `greet` that prints “Hello”

// In the global scope, call the `greet` function, like this:
// greet()
































// Task 2
// Change the `greet` function so that it takes one parameter
// called `name`.
// Change the function body so that it prints “Hello” followed
// by a space then followed by the value of the `name` variable.

// In the global scope, call the greet function like this:
// greet("Beth")


































// Task 3
// Change the previous function so that it also takes a second
// parameter called `printCount`. Change the function body so
// that it prints the greeting `printCount` times.

// In the global scope, call the greet function like this:
// greet("Beth", 5)


































// Task 4
// Write a new function called `double` that takes one parameter,
// called `number` and returns number * 2.

// In the global scope, call the `double` function and then print
// it’s return value:
// const result = double(3);
// console.log(result);

// Does it make a difference if we call the function as below?
// console.log(double(3));


































// Task 5
// In the global scope, write code that prints the double of the
// first 10 integers. Use the `double` function for the solution.
// Try to write this in the smallest amount of code possible.

