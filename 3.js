
// Task 1
// Write a function called `doTwice`. This function should take a
// parameter called `fn`. The `doTwice` function should call `fn`
// two times. (Yes, `fn` is a function, and it’s also a variable)

// Let’s call this function like this:
// doTwice(() => {
// 	console.log("It’s an arrow function");
// });




























// Task 2
// Write a function called `repeat`. This function should take a
// parameter called `fn` and another parameter called `count`.
// The `repeat` function should call `fn`, `count` times.

